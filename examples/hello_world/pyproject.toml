[project]
name = "hello_world"
version = "0.1.0"
description = "A simple Flask application for testing TestGen"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=68.0.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["."]